apiVersion: v1
kind: Service
metadata:
  name: {{ include "debitos-tributarios.fullname" . }}
  labels:
    {{- include "debitos-tributarios.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "debitos-tributarios.selectorLabels" . | nindent 4 }}
